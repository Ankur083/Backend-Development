<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-zinc-500" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post"> 
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none bg-transparent" type="text" name="name" value="<%= user.name %>" placeholder="name">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none bg-transparent" type="text" name="email" value="<%= user.email %>" placeholder="email">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none bg-transparent" type="text" name="image" value="<%= user.image %>" placeholder="image_url">
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-xl" type="submit" value="update user">

        </form>
    </div>
</body>
</html>